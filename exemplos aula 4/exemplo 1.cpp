#include <stdio.h>

int main() {
    setlocale(LC_ALL, "Portuguese");
    int x = 0;
        while (x !=5){
            printf("Valor de X = %d \n", X);
            X = X + 1;
    }
    printf("\nValor de X depois que sair da estrutura = %d",X);
}